# TODO

1. **`@command` decorator for help** — self-decorating commands, auto-registration, remove the manual `HELP` string from `bot.py`
2. **`bot.py` heartbeat typo** — "I am up1!" → "I am up!"
3. **Planner task enhancements** — bulk add (multi-line, one task per line), bulk delete (`/deltasks 1 2 3`), optional due date on tasks; scheduling algo should factor in due dates
4. **`/week` command** — sends this week's plan as an image; eventually pull from Canvas (design TBD)
5. **Money tracker** — new module, separate from planner (design TBD)
